## 2024-06-10 - ARIA labels for icon-only buttons
**Learning:** Icon-only buttons often use the `title` attribute for tooltips but frequently omit the `aria-label` attribute, which is necessary for screen reader accessibility in the document search UI.
**Action:** Always pair `title` with `aria-label` when creating icon-only buttons to ensure they are accessible to all users.
