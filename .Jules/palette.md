## 2026-05-30 - Add ARIA label to PropertyFrom close button
**Learning:** Found a missing accessible label in an icon-only button inside the LSP extension settings.
**Action:** Always ensure `trans` is passed down to smaller functional components to localize `title` and `aria-label` attributes.
